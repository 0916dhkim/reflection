{
  "name": "@reflection/backfill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/backfill.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist/backfill.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode/schema": "2.0.8",
    "effect": "4.0.0-rc.112",
    "@reflection/shared": "workspace:*"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
