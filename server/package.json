{
  "name": "@reflection/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist/main.js --external:fastify --external:@fastify/swagger --external:@fastify/swagger-ui --external:pg",
    "start": "node dist/main.js",
    "test": "vitest run --exclude 'test/*.integration.test.ts'",
    "test:integration": "node test/dual-plugin-fixture/database-guard.mjs && pnpm --filter opencode-reflection-plugin build && pnpm --filter @reflection/opencode-v2-plugin build && vitest run test/database.integration.test.ts test/dual-plugin.integration.test.ts --no-file-parallelism --config=./test/dual-plugin-fixture/sequencer.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/swagger": "9.8.1",
    "@fastify/swagger-ui": "6.1.1",
    "@reflection/shared": "workspace:*",
    "fastify": "5.12.1",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@types/pg": "8.23.1"
  }
}
